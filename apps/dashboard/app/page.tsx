import { dashboardTitle } from './constants';

export default function HomePage() {
  return (
    <main>
      <h1>{dashboardTitle}</h1>
      <p>Repository foundation is ready for later milestones.</p>
    </main>
  );
}
