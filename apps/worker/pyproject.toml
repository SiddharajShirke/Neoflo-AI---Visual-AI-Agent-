[project]
name = "visual-ai-worker"
version = "0.0.0"
requires-python = ">=3.11,<3.12"
dependencies = ["visual-ai-shared"]

[project.scripts]
visual-ai-worker = "visual_ai_worker.__main__:main"

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"
